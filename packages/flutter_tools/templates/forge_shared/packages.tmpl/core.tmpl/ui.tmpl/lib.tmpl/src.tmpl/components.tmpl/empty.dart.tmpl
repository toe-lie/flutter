import 'package:flutter/material.dart';

class {{pascalCaseProjectName}}EmptyView extends StatelessWidget {
  const {{pascalCaseProjectName}}EmptyView({super.key, required this.message});
  final String message;

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(message, textAlign: TextAlign.center,),
    );
  }
}
